# Review — v0.15.0 Event list filters & quieter cards

Date: 2026-08-06  
Scope: Event list filter/sort bar; icon-only event actions; drop public PUBLISHED label

## Architecture

- Filtering/sorting is client-side over the published event payload already loaded for `/events` (same pattern as map filters).
- No API contract change; repository default order remains `startAt asc`.

## Code quality

- Pure helpers in `event-list-filters.ts` with unit tests.
- Event actions use shared `button` `icon` size with `title` + `aria-label`.

## Follow-ups

- Optional: push category/region/date/sort query params to the public events API for large catalogs.
- Optional: URL-synced filter state for shareable list views.
