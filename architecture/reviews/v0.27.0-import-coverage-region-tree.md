# Review — v0.27.0 Import coverage region tree

Date: 2026-08-20  
Scope: Admin Import settings coverage UI — hierarchy tree + parent selects descendants

## Architecture

- Reuses `region-tree.ts` (`buildRegionForest`, `flattenVisible`, expand/collapse) plus `collectSubtreeIds` for checkbox cascade.
- Stored coverage roots still expand at filter time (`expandCoverageRegionIds`); UI expands roots on load so child checkboxes match the effective scope.

## Product

- Operators pick Abdeckungsgebiet in Land → … → Ort tree; parent click selects/deselects entire subtree (indeterminate for partial).

## Follow-ups

- Optional: same tree UX for category import allowlist when categories gain deeper nesting.
