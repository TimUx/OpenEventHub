# Review — Portal UX, Map, Submit, Network Hardening (v0.12.0)

Date: 2026-08-05  
Scope: Post-M11 product/ops follow-ups (not a new roadmap package number)

## Architecture

- Docker networks remain hexagonal at the edge: Traefik is the only host entrypoint (`edge`); data plane and workers stay on `internal` (`internal: true`).
- Public API event payloads now include `venue` coordinates and `categories`, enabling map markers without core schema changes.
- Public submissions continue to flow through moderation (`POST /api/v1/submissions`, `source-submissions`) — no bypass of Admin RBAC.

## Code

- Frontend: flat FestSchmiede-inspired tokens; Leaflet map with client-side filter + `fitBounds`; `/submit` forms; calendar/event view modes; `de`/`en` i18n.
- Admin: matching flat chrome (solid primary app bar).
- Compose/Stack/Monitoring aligned on network policy; optional `docker-compose.dev-ports.yml` for local tooling.
- Migrate/seed scripts attach to `${COMPOSE_PROJECT_NAME}_internal` instead of host networking.

## Docs / QA

- DE+EN docs updated (VISION, ROADMAP Post-M11, ARCHITECTURE, CONTAINERS, COMMUNICATION, DOCKER_*, FRONTEND, ADMIN, WIREFRAMES, README).
- Screenshots regenerated under `docs/assets/screenshots/` (12 images).
- `validate:compose`, `validate:stack`, `validate:monitoring` pass; frontend/api/database tests for map-events and public event mapping covered.
