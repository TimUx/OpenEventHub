# Review — v0.25.0 Verified regions, Admin region tree & AI Ollama fix

Date: 2026-08-20  
Scope: Region ingest verification; Admin Regions bulk + hierarchy tree; AI provider DNS/`fetch failed`

## Architecture

- AI taxonomy-link resolves places via catalog match or Nominatim settlement/admin filter only; venues stay venues; no blind LLM region create. Shared helpers: `isSettlementOrAdminNominatimHit`, `pickBestSettlementCandidate`. Classification prompt **1.0.6**.
- Admin Regions: `PATCH …/bulk-parent`, `POST …/bulk-delete` (max 200, all-or-nothing; delete refuses nodes with children). Client tree (`region-tree.ts`) builds forest from flat `GET /admin/regions`.
- LLM `postJson` surfaces undici `cause` (e.g. `ENOTFOUND ollama`); AI queue concurrency 1. Seed/default Ollama URL prefers `host.docker.internal` when Compose hostname `ollama` is absent.

## Product

- Operators see Land → Ort hierarchy and can bulk-reparent/delete leaves.
- Crawl/AI no longer invents church/parking regions; external Ollama on host port works without `fetch failed`.

## Follow-ups

- After deploy: set Admin AI profile URL/model if still on `http://ollama:11434/v1`; re-queue failed AI jobs; optional `bash scripts/repair-dev-regions.sh` on DEV.
- Hierarchy tree: drag-and-drop reparent remains out of scope (bulk parent is the path).
