# Review — v0.14.1 Compact header controls

Date: 2026-08-06  
Scope: Quieter language + accent controls in portal/admin headers

## Architecture

- No new services or APIs; client-only UI polish.
- Locale still toggles via existing cookie/`setLocale`; accent menu still uses `oeh-accent` localStorage.

## Code quality

- Language and theme/accent share the same icon-button chrome as dark/light.
- Accent names remain in the flyout for discoverability; trigger is a palette icon matching language/theme.

## Follow-ups

- Optional: keyboard listbox navigation in the accent menu.
